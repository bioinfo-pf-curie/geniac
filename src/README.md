# Files for Geniac command line interface

Geniac comes with a command line interface including `lint`, `install`, `configs` and `clean` utilities. They have been wriitten in python. This folder contains the source code  for the python package.
