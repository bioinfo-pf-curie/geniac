# Files for cmake

Geniac provide a utility to configure and install nextflow pipeline using cmake. The cmake functions are provided in this folder.

The main script is `CMakeLists.txt`, it content details what are the differents steps.

The script `initCmakePreload.sh` allows the generation of the cmake file with all the available options and CACHE values.
