# Files for the nextflow pipeline launched by geniac

Geniac launches a nextflow pipeline which generates config files, container recipes and build the containers. The nextflow pipeline is in this folder.
